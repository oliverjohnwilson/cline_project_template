# Application settings (loaded by src/cline_project_template/config/settings.py via Pydantic)
# Copy this file to .env (do NOT commit .env). This example is committed safely for reference.
# See .clinerules/070-security-standards.md for secret handling policy.

# Environment name: dev | test | prod
APP_ENV=dev

# Base URL for an external API (example)
# Example: https://api.example.com
APP_API_BASE=https://api.example.com

# Retry limit for operations (integer: 0-10)
APP_RETRY_LIMIT=3

# Feature flags
APP_FEATURE_X_ENABLED=false

# PyPI publishing (used by release workflow if using legacy Twine credentials; prefer Trusted Publishing)
# TWINE_USERNAME=__token__
# TWINE_PASSWORD=your-pypi-token

# Optional: Python and virtualenv config for local scripts (dev.sh, release.sh)
PYTHON_BIN=python3.11
VENV_DIR=.venv
